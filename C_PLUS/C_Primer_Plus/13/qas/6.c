/*************************************************************************
	> File Name: 6.c
	> Author: Crow
	> Mail: qnglsk@163.com 
	> Created Time: Fri Sep 25 10:57:28 2020
 ************************************************************************/

#include<stdio.h>
#include<stdlib.h>
#include<time.h>
#include<string.h>

/*
 * 6.  What’s the difference between binary files and text files on the one hand versus binary 
 * streams and text streams on the other? 
 *
 * */


/*
 *
 *
 The distinction between a binary file and a text file is a system-dependent difference 
 between file formats. The distinction between a binary stream and a text stream consists 
 of translations performed by the program as it reads or writes streams. (A binary stream 
 has no translations; a text stream may convert newline and other characters.)
 *
 *
 * the distinction between a binary file and a text file is a system-dependent difference
 * between file formats.
 * the distinction between a binary stream and a text stream consists of translations
 * performed by the program as it reads or writes streams.
 *
 * A binary stream has no translations
 * a text stream may convert newline and other characters.
 *
 *
 *
 * the distinction between a binary file and a text file is a 
 * system-dependent difference between file formats.
 *
 * the distinction between a binay stream and a text stream 
 * consists of translations performed by the program 
 * as it reads or writes streams.
 *
 * A binary stream has no translations,
 * a text stream may convert nerline and other characters.
 *
 *
 *
 *
 * the distinction between a binary file and a text file is 
 * a system-dependent between file formats.
 * the distinction between a binary stream and a text stream consists of
 * translations performed by program as it reads and writes streams.
 *
 * A binary stream has no translations
 * a text stream may convert nerline and other characters.
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 *
 * */

